cabar:
  version: v1.0

  component:
    version: v1.0
    description: 'ruby_is_forked - Create new DB connections in forked children.'

  provides:
    lib/ruby: lib
    tests: spec

  requires:
    component:
      ruby: true
